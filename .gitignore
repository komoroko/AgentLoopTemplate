# ---- Python / uv ----
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
build/
dist/
# Commit uv.lock for reproducibility (do not ignore it)

# ---- frontend / pnpm ----
frontend/node_modules/
frontend/dist/
frontend/.vite/

# ---- mkdocs ----
site/

# ---- AgentLoop runtime artifacts ----
# (.agentloop/events.ndjson — the structured escalation log — is deliberately NOT ignored: created
#  on the first event, it is the machine-readable truth behind state.md's generated view, meant to
#  be committed with the cycle's records and archived at cycle-close.)
.worktrees/                 # temporary worktrees build_loop.py creates for parallel tasks
.agentloop/build-loop.log   # the pre-events escalation log (legacy; no longer written)
.agentloop/build-loop.log.1 # its rotated previous generation
.agentloop/build-loop.lock  # the single-run PID lock
.agentloop/pr-draft.md      # generated PR body (make pr-draft); regenerate rather than track

# ---- environment variables / secrets ----
.env
.env.*
!.env.example

# ---- OS / editor ----
.DS_Store
.idea/
.vscode/

# ---- Claude Code local settings ----
.claude/settings.local.json
