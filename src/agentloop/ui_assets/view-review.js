// Review: read the gate's deliverables, then approve — filled in by the gate-review feature.

export function renderReview() {
  const el = document.getElementById("review");
  el.innerHTML = '<div class="empty">Gate review is coming in a following change.</div>';
}
